spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/yly?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai
    username: root
    password: 123456
    hikari:
      maximum-pool-size: 20
      minimum-idle: 8
  servlet:
    multipart:
      max-file-size: 5MB
      max-request-size: 50MB
  mail:
    host: smtp.qq.com
    port: 465
    username: 2388564445@qq.com
    password: wkzrzkyrxiuodhhh
    properties:
      mail:
        smtp:
          ssl:
            enable: true

  data:
    redis:
      host: 127.0.0.1
      port: 6379

logging:
  level:
    com.neuedu.mapper: debug

minio:
  endpoint: http://192.168.80.110:9000
  accesskey: admin
  secretkey: 12345678

ignore:
  white:
    urls:
      - /admin/login
      - /user/login
      - /user/add
      - /user/regist
      - /user/check
      - /success
      - /sysFile/upload
      - /nurse/*
      - /elderCheck/*

admin:
  urls:
    - /adminHome

allow:
  origin: http://127.0.0.1

mybatis-plus:
  global-config:
    db-config:
      logic-delete-field: isDeleted   # 全局逻辑删除字段名
      logic-delete-value: 1           # 删除后的值
      logic-not-delete-value: 0       # 未删除的值